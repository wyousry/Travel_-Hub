import 'package:flutter/material.dart';

class AiCamera extends StatelessWidget {
  const AiCamera({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}